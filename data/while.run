
i = Number(input("Enter a number of repetitions: "))

loop = {
    [1]i = i - 1
    print("Number of repetitions left: " + i)

    branches = [{ loop() }, { print("Loop ended") }]
    branches[i == 0]()
}

loop()